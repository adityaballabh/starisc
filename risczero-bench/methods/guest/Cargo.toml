[package]
name = "guest"
version = "0.1.0"
edition = "2021"

[workspace]

[[bin]]
name = "fib"
path = "src/fib.rs"

[dependencies]
risc0-zkvm = { version = "^3.0.5", default-features = false, features = ['std'] }
